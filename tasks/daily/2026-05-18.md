# Tasks — 2026-05-18

## To Do

### Adapters
- [x] Build Salesforce adapter (`src/lib/adapters/salesforce.ts`) — REST API + Tooling API for schema, flows (Process Builder / Flow), user stats, and duplicate rules; register in `createAdapter()` factory
- [x] Build HubSpot adapter (`src/lib/adapters/hubspot.ts`) — Private App token auth; CRM Properties API for schema, Workflows API for automation, Contacts API for data quality, Users API for adoption

### D365 Phase 2 gaps (marked `// phase 2` in `src/lib/adapters/d365.ts`)
- [x] Admin role query — fetch users with System Administrator role to populate `adminUsers` and `dormantAdmins` (no login in 90d)
- [x] Azure AD MFA check — call Microsoft Graph `/reports/credentialUserRegistrationDetails` to populate `usersWithoutMFA`
- [x] Audit log status — read `organization.IsAuditEnabled` from Dataverse org settings to populate `auditLogEnabled`
- [x] Field-level stats — query `AttributeDefinitions` per entity to fill `totalFields` / `unusedFields`

### Backend
- [x] Add `server/routes/sequences.ts` — CRUD for sequences and steps (schema tables already exist: `sequences`, `sequence_steps`); mount in `server/index.ts`

### Cleanup
- [x] Delete `src/pages/sales/Contacts.tsx` — no longer routed or imported after today's refactor

## In Progress

## Done
- All tasks complete
