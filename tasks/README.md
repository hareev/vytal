# Tasks

Daily task files live in `daily/`, named `YYYY-MM-DD.md`.

Add a new file each day with tasks to complete. Each file has three sections:
- **To Do** — not started
- **In Progress** — actively being worked on
- **Done** — completed
